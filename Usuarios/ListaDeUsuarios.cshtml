@model List<Usuario>
 <h1> Usuarios cadastrados no sistema</h1>

 <p class="text-danger">@ViewData["mensagem"]</p> 

 <table class="table">
     <thread>
         <tr>
             <th scope="row">Nome</th>
             <th scope="row">Login</th>
             <th scope="row">Tipo</th>
         </tr>
     </thread>

     <tbody>
         @foreach (Usuario u in Model)
         {
          <tr>
            <td>@u.Nome</td> 
            <td>@u.login</td>
            @if(u.tipo==Usuario.ADMIN)
            {
                <td>Administrador</td>
            } 
            else
            {
                <td>Padrão</td>
            }


            <td><a href="editarUsuario?id=@u.Id">editar</a></td>
            <td><a href="ExcluirUsuario?id=@u.Id">excluir</a></td>

          </tr>   
         }

     </tbody>
 </table> 

 <a href="RegistrarUsuarios">Registras novo usuário</a> 